define(
    [
        
        //'underscore',
        'backbone',
        
        'model'
    ],
    function (Backbone, model ) {
        
        model.Experience = Backbone.Model.extend( {
            
            
        
        } );

        return model.Experience;
    }
);